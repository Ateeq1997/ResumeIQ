# Gemini API Key (https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=AQ.Ab8RN6IkNdtZFtn3Q_-j_KAcNC94eMcF3WYAUwOz-eEeYqO_BA

# Optional: model name override
GEMINI_MODEL=gemini-1.5-flash

# CORS origin for frontend (set to your Vercel deployment URL in prod)
FRONTEND_ORIGIN=http://localhost:5173

# Storage path (relative to api/ on serverless this is ephemeral; use /tmp in prod)
HISTORY_FILE=storage/resume_history.json
